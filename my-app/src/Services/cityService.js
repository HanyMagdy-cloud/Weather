// cityService.js

/*export class CityService {
    constructor(cities) {
        this.cities = cities; // Predefined list of cities
        this.favoriteCities = []; // Array to store favorite cities
    }

    updateFavoriteList(searchTerm) {
        return this.cities.filter(city => city.toLowerCase().includes(searchTerm.toLowerCase()));
    }

    saveCity(city) {
        if (!this.favoriteCities.includes(city)) {
            this.favoriteCities.push(city);
        }
    }

    getFavoriteCities() {
        return this.favoriteCities;
    }
}*/